const fetch = require('node-fetch');
async function test() {
  const url = 'https://mysafawala.com/api/whatsapp/send-invoice';
  // I need a valid booking payload to send to this endpoint, but I can also just check the response
  console.log("We need to simulate a request or check vercel logs.");
}
test();
